#!/bin/sh
cd "$(dirname "$0")/.." || exit 1
if command -v mise >/dev/null 2>&1; then
  exec mise exec -- pnpm run precommit
elif command -v pnpm >/dev/null 2>&1; then
  exec pnpm run precommit
else
  exec npx --yes pnpm@10.33.0 run precommit
fi
